language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install pipenv
  - pipenv install
  - pipenv run python encodertb/pyencoder/builder.py
# command to run tests
script: pytest